fastapi>=0.110
uvicorn[standard]>=0.29
numpy>=1.26
pydantic>=2.6
pytest>=8.0
httpx>=0.27

# Optional — only needed if you want to load a trained PointNet++
# checkpoint (models/checkpoints/pointnet2.pth). The backend runs fully
# without it, using the deterministic label-driven perception fallback.
# torch>=2.2
# open3d>=0.18
